diff --git a/CMakeLists.txt b/CMakeLists.txt
index ae1c0fc..a490a12 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -40,6 +40,10 @@ endif()
 
 file(GLOB INTERFACES "interfaces/*.h")
 
+# Build the Qt 6 dbusmenu implementation bundled with GXDE Dock.  The source
+# is copied into this checkout by the installer compatibility step.
+add_subdirectory("cmake/libdbusmenu")
+
 #add_subdirectory("frame")
 add_subdirectory("plugins")
 
diff --git a/plugins/tray/CMakeLists.txt b/plugins/tray/CMakeLists.txt
index b2f52e5..509b431 100644
--- a/plugins/tray/CMakeLists.txt
+++ b/plugins/tray/CMakeLists.txt
@@ -21,7 +21,6 @@ find_package(Dtk6Widget REQUIRED)
 find_package(Dtk6Gui REQUIRED)
 find_package(Dtk6Core REQUIRED)
 find_package(DFrameworkdbusQt6 CONFIG REQUIRED)
-find_package(dbusmenu-lxqt CONFIG REQUIRED)
 
 pkg_check_modules(XCB_LIBS REQUIRED xcb-ewmh xcb xcb-image xcb-composite xtst x11 xext xcb-icccm)
 pkg_check_modules(DDE-Network-Utils REQUIRED gxde-network-utils-qt6)
@@ -34,9 +33,12 @@ target_include_directories(${PLUGIN_NAME} PUBLIC ${DtkGui_INCLUDE_DIRS}
                                                  ${XCB_LIBS_INCLUDE_DIRS}
                                                  ${DDE-Network-Utils_INCLUDE_DIRS}
                                                  ${QGSettings_INCLUDE_DIRS}
-                                                 ${dbusmenu-lxqt_INCLUDE_DIRS}
                                                  ../../interfaces
                                                  ../../frame)
+target_include_directories(${PLUGIN_NAME} PRIVATE
+    "${CMAKE_SOURCE_DIR}/lib/3rdparty/libdbusmenu/src"
+    "${CMAKE_BINARY_DIR}/cmake/libdbusmenu/dbusmenu-build/src"
+)
 target_link_libraries(${PLUGIN_NAME} PRIVATE
     Dtk6::Gui
     Dtk6::Widget
@@ -46,7 +48,6 @@ target_link_libraries(${PLUGIN_NAME} PRIVATE
     Qt6::Svg
     ${XCB_LIBS_LIBRARIES}
     ${DDE-Network-Utils_LIBRARIES}
-    #${dbusmenu-lxqt_LIBRARIES}
     dbusmenu-lxqt
     DFrameworkdbusQt6::DFrameworkdbusQt6
     ${QGSettings_LIBRARIES}
